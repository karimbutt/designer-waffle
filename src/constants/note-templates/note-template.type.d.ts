import { PartialBlock } from '@blocknote/core';

type NoteTemplate = {
  name: string;
  title: string;
  body: PartialBlock[];
};

// type NoteTemplate = {
//   name: string;
//   title: string;
//   body: string;
// };
